name: Feature request
description: Propose a new capability (physics model, backend, binding, tooling)
labels: ["enhancement"]
body:
  - type: textarea
    id: problem
    attributes:
      label: What problem does this solve?
      description: The use case or limitation motivating the request.
    validations:
      required: true
  - type: textarea
    id: proposal
    attributes:
      label: Proposed capability
      description: What you'd like Cyberfluids to do. Larger features are developed spec-first with OpenSpec — see CONTRIBUTING.md.
    validations:
      required: true
  - type: dropdown
    id: area
    attributes:
      label: Area
      options:
        - Physics / dynamics
        - Boundary conditions / geometry
        - Backend (CPU / CUDA / OpenCL / Metal / SYCL)
        - Language bindings (Python / Swift)
        - Build / packaging / tooling
        - Documentation
        - Other
    validations:
      required: true
  - type: textarea
    id: alternatives
    attributes:
      label: Alternatives considered
      description: Other approaches or existing workarounds, if any.
